// Variables
int counter = 50;
int totalNumber = 0;

// Loop
while (counter >= 50 && counter <= 150) {
    // Total Number
    totalNumber += counter;

    // Counter
    counter++;
}

// Write
Console.WriteLine($"Total Number: {totalNumber}");
